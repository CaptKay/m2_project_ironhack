
{
  "name": "interactive-stories-backend",
  "version": "1.0.0",
  "description": "Backend for Interactive Stories App",
  "main": "server.js",
  "scripts": {
    "start": "json-server --watch db.json --port 3001 --routes routes.json",
    "dev": "json-server --watch db.json --port 3001 --routes routes.json"
  },
  "dependencies": {
    "json-server": "^0.17.3"
  }
}